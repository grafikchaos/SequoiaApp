<% content_for :page_title, "Entity Types" %>
<% content_for :display_title, "Entity Types" %>

<%= render 'shared/admin_tabs' %>

<div class="controls">
  <% if can? :create, EntityType %>
    <%= link_to 'New Entity Type', new_entity_type_path, :class => 'button' %>
  <% end %>
</div>
<div class='clear'></div>

<table>

<% @entity_types.each do |entity_type| %>
  <tr>
    <td><%= entity_type.name %></td>
    <td>
      <%- if can? :edit, EntityType -%>
        <%= edit_link edit_entity_type_path(entity_type), :id => "edit-#{entity_type.friendly_id}", :title => 'Edit this entity type' %>
      <%- end -%>
    </td>
    <td>
      <%- if can? :destroy, EntityType -%>
        <%= destroy_link entity_type, :id => "delete-#{entity_type.friendly_id}", :title => 'Delete this entity type' %>
      <%- end -%>
    </td>
  </tr>
<% end %>
</table>
