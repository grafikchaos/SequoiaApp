<%= form_for(@entity_row) do |f| %>
  <% if @entity_row.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entity_row.errors.count, "error") %> prohibited this entity_row from being saved:</h2>

      <ul>
      <% @entity_row.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
