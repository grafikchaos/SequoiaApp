<h1>Listing entity_rows</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @entity_rows.each do |entity_row| %>
  <tr>
    <td><%= link_to 'Show', entity_row %></td>
    <td><%= link_to 'Edit', edit_entity_row_path(entity_row) %></td>
    <td><%= link_to 'Destroy', entity_row, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Entity row', new_entity_row_path %>
