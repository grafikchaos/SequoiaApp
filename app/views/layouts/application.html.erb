<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title) %> | August Ash Intranet</title>
  <%= stylesheet_link_tag "web-app-theme/base", "web-app-theme/themes/warehouse/style", "styles", :cache => true %>
  <%= javascript_include_tag :defaults, :cache => true %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="header">
      <h1><a href="/"><%= yield(:title) %></a></h1>
      <div id="user-navigation">
        <ul class="wat-cf">
          <li><%= link_to 'Logout', destroy_user_session_path %></li>
        </ul>
      </div>
      <div id="main-navigation">
        <ul class="wat-cf"></ul>
      </div>
    </div>
    <div id="wrapper" class="wat-cf">
      <div id="main">
        <%= yield %>
      </div>
      <div id="sidebar">
        <% if content_for?(:sidebar) %>
          <%= yield :sidebar %>
        <% else %>
          <%= render 'shared/sidebar' %>
        <% end %>
      </div>
    </div>
    <%= render 'shared/footer'%>
  </div>
  <!-- Need flash message here -->
</body>
</html>
