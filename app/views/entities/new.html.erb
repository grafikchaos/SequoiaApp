<%= content_for :page_title, "New Entity" %>

<section class="block">
  <section class="content">
    <header><h2>New Entity</h2></header>
    <section class="inner">
      <%= form_for([@client, @entity], :html => { :class => 'form' }) do |f| %>
        <%= render 'form', :f => f %>
      <% end %>
    </section>
  </section>
</section>
