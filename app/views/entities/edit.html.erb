<%= content_for :page_title, "Editing Entity" %>
<%= content_for :display_title, "Editing \"#{@entity.name}\"" %>

<%= form_for(@entity) do |f| %>
  <%= render 'form', :f => f %>
<% end %>

<%= link_to 'Back', @client %>
