<%= content_for :page_title, "New Entity" %>
<%= content_for :display_title, "New Entity" %>

<%= form_for([@client, @entity]) do |f| %>
  <%= render 'form', :f => f %>
<% end %>

<%= link_to 'Back', @client %>
